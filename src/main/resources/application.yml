logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE
server:
  port: 8080
  servlet:
    context-path: /test
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 12345dsh
    url: jdbc:mysql://localhost:3306/medicine?useUnicode=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect